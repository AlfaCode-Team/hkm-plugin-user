/vendor/
composer.lock
.phpunit.cache/
.phpunit.result.cache
.DS_Store

# AI assistant context — kept local, not shipped to GitHub
CLAUDE.md

# Local plugin-development install (bin/link-local) — regenerable, machine-local
composer.local.json
composer.local.lock

# UI component tests: __tests__/ and __fixtures__/ ARE source and belong in git.
# node_modules and the lockfile are not; the vitest config and package.json are
# regenerated by `hkm-plugin-ground make:ui-test --config`.
/ui/node_modules/
/ui/package-lock.json
node_modules

# ── Local test harness (`hkm ground init`) ──────────────────────────
# Machine-specific, regenerable, and some of it holds credentials.
# The TESTS themselves (tests/, ui/__tests__, ui/__fixtures__) are
# source and belong in git — only the scaffolding below does not.
# database hosts + PASSWORDS for `ground migrate`
ground.databases.json
# local database servers
docker-compose.ground.yml
# generated by `ground new ui-test --config`
/ui/package.json
# generated; its aliases are this machine's checkouts
/ui/vitest.config.ts
/vendor/

# ── Local test harness (`hkm ground init`) ──────────────────────────
# Machine-specific, regenerable, and some of it holds credentials.
# The TESTS themselves (tests/, ui/__tests__, ui/__fixtures__) are
# source and belong in git — only the scaffolding below does not.
# generated dev server workspace (`ground dev`)
/ui/.ground/

# ── Local test harness (`hkm ground init`) ──────────────────────────
# Machine-specific, regenerable, and some of it holds credentials.
# The TESTS themselves (tests/, ui/__tests__, ui/__fixtures__) are
# source and belong in git — only the scaffolding below does not.
# generated by yarn install
/ui/yarn.lock
# generated by pnpm install
/ui/pnpm-lock.yaml
# generated alias stub for the shared UI kit
/ui/tsconfig.plugins.json
# vite build output
/ui/dist/
# vite dependency cache
/ui/.vite/

# ── Local test harness (`hkm ground init`) ──────────────────────────
# Machine-specific, regenerable, and some of it holds credentials.
# The TESTS themselves (tests/, ui/__tests__, ui/__fixtures__) are
# source and belong in git — only the scaffolding below does not.
# local env for `ground` — may hold real keys
.env
